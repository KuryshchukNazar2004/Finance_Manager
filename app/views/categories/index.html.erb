<main>
  <div class="album py-5 bg-light">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3" id ='categories'>
      <% @categories.each do |category| %>
          <%= render 'index_category', category:category %>
      <% end %>
    </div>
  </div>
</main>
