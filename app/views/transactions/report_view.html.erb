<main>
  <div class="d-flex justify-content-center">
    <h3>Total price - <%= Transaction.sum(:sum) %> grn</h3>
  </div>
  <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom"> 
    <%= pie_chart Transaction.group_by_day(:created_at).sum(:sum)%>
    <%= column_chart Transaction.group_by_day(:created_at).sum(:sum)%>
  </div>
</main>
